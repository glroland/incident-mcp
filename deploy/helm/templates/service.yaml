apiVersion: v1
kind: Service
metadata:
  name: {{ include "incident-mcp.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "incident-mcp.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "incident-mcp.selectorLabels" . | nindent 4 }}
